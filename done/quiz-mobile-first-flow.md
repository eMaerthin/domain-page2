# Done: Mobile-first quiz flow

## Implemented
- Added `Question.tsx` with hero image, large thumb-friendly answers, feedback colors, haptic tap, and auto-next delay.
- Added `useQuizEngine()` and `QuizFlow.tsx`.
- Wired `QuizFeed` to the new flow.
- Added mobile-first styling for the quiz experience.

## Notes
- This was the first pass at making quiz content conversion-oriented on mobile.
- It passed local `typecheck` and `build` before deployment.
